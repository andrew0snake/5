#include <stdio.h>

void main () {

    int A [ ] [ 3 ] = { 1, 12, 23,   34, 45, 56,   67, 78, 89 };

    int ( * p ) [ 3 ] = A;

    int i = 0;

    printf ( "p [ 0 ] [ 2 ] = %d;\n", p [ 0 ] [ 2 ] );
    printf ( "* ( * ( p + 0 ) + 2 ) = %d;\n\n", * ( * ( p + 0 ) + 2 ) );

    printf ( "* ( * ( A + 0 ) + 2 ) = %d;\n", * ( * ( A + 0 ) + 2 ) );
    printf ( "* ( * ( A + 1 ) + 2 ) = %d;\n", * ( * ( A + 1 ) + 2 ) );
    printf ( "* ( * ( A + 2 ) + 2 ) = %d;\n", * ( * ( A + 2 ) + 2 ) );
    printf ( "* ( * ( A + 3 ) + 2 ) = %d;\n\n", * ( * ( A + 3 ) + 2 ) );

    for ( i = 0; i < 7; i ++ ) {
        printf ( "* ( * ( A + ( i = %d ) ) + 2 ) = %d;\n", i, * ( * ( A + i ) + 2 ) );
    }


}
